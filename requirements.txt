# ============================================================================
# Dépendances Python de Kira.
# Installe avec :  pip install -r requirements.txt
# ============================================================================

# --- Cœur agentique (couche Claude Code, section 8) -------------------------
anthropic>=0.40.0        # backend du moteur de raisonnement (API Claude)
pyyaml>=6.0              # chargement de policy.yaml (couche sécurité)

# --- Tests ------------------------------------------------------------------
pytest>=8.0

# --- Pont MCP (canal de communication Cursor <-> Claude Code) ---------------
mcp>=1.0                 # SDK serveur MCP (mcp_bridge/server.py)
filelock>=3.0            # accès concurrent au store de messages

# --- Apprentissage / mini-GPT (couche Cursor, section 7-8) ---------------
torch>=2.0.0
# tiktoken optionnel (SSL/reseau) — phase 2 utilise learn/bpe_local.py par defaut

# Note : python-dotenv n'est PAS requis — kira/engine.py embarque un petit
# chargeur .env autonome. Tu peux quand même l'installer si tu préfères.
